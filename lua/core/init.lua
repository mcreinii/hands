-- Setup default options
require("core.options").setup()
require("core.options").relative_num()
require("core.options").explicit_rn()

-- Setup default keymappings
require("core.keymaps").setup()

require("core.colorscheme").setup("vague")
